const n=`<template>
  <LlmArticle>
    <div class="llm-question"><strong>开场问题：</strong>为什么一百层直线叠起来还是直线？——给网络引入「弯折」。</div>

    <h2>提出问题：多叠几层，真的更强吗？</h2>
    <p>上一课那台猜水果的机器输出端挂着一个 σ，当时一笔带过说它「把总分压成概率」。这一课正面回答：这个 σ 到底是什么、为什么非有它不可。其实这堵「天花板」撞见两次了：第 4 课发现「线性叠线性还是线性」，第 7 课又卡在「单个神经元只能切一条直线」。做个实验：把两台水果神经元前后接起来（两层），中间<strong>不放任何激活函数</strong>。第一层 <code>h = W₁x + b₁</code>，第二层 <code>y = W₂h + b₂</code>，代入得 <code>y = (W₂W₁)x + (W₂b₁ + b₂) = W′x + b′</code>。W₂W₁ 合并成新矩阵，W₂b₁＋b₂ 合并成新偏置——<strong>两层网络等价于一层网络</strong>。继续叠三层、一百层，最终都能压成「一个矩阵 + 一个偏置」。层数带来的只有运算量、没有表达能力。</p>

    <h2>「一层」意味着什么：永远是直线</h2>
    <p>既然多层纯线性网络等价于一层，就看这一层能做什么。二维空间里 <code>y = Wx + b</code> 画出一条直线（高维里是超平面）。回到 XOR：(0,0)否、(1,0)是、(0,1)是、(1,1)否——无论把直线摆哪个角度都分不开两类点。推广：圆形区域、月牙形、任意不规则边界，纯线性网络全部做不到。打个比方：线性变换只能把整张纸拉伸、旋转、平移，纸始终是平的；非线性则像在纸上<strong>「折」一道弯</strong>，折过几次后，原本被一条直线分开的两类点就能被叠进同一侧或圈进不同区域。办法已经呼之欲出。</p>

    <h2>最小方案：层之间加「弯折」</h2>
    <p>这个「插在层与层之间的非线性函数」正是本课主角——<strong>激活函数（activation function）</strong>。它紧跟在每一层的线性变换 Wx+b 之后，给网络添上一道弯折，「多层＝一层」的魔咒就此打破。</p>

    <h2>发现不足 / 候选逐个看</h2>
    <ul>
      <li><strong>阶跃函数</strong>（z&lt;0 输出 0，z≥0 输出 1）：确实「弯」了，但太死板——除了 z=0 的跳变点，其余处完全平坦、输出对输入毫无反应，无法精细调整，几乎没人用。</li>
      <li><strong>Sigmoid</strong> <code>σ(z)=1/(1+e⁻ᶻ)</code>：把阶跃「平滑」成 S 型曲线，输出 0~1 可当概率，一度最流行；但两端会进入<strong>饱和区</strong>（z 很大或很小时曲线几乎水平、变得迟钝），网络深了迟钝会累积拖慢学习。</li>
      <li><strong>tanh</strong>：和 Sigmoid 同是 S 形，区别在输出 <code>−1~1</code>、以 0 为中心，每层输出有正有负更均衡，通常更好训练；但 <code>tanh(z)=(eᶻ−e⁻ᶻ)/(eᶻ+e⁻ᶻ)</code> 两端一样会饱和。</li>
      <li><strong>ReLU</strong> <code>max(0, z)</code>：正数原样放过、负数归零，正区间导数恒为 1、毫不衰减，又快又稳，是现代深层网络（ResNet、BERT、GPT）隐藏层的默认。小毛病是 z&lt;0 时输出恒 0、神经元「躺平」难唤醒，于是有改良版 <strong>LeakyReLU</strong>（负区间留一点微弱斜率防躺平）和 <strong>GELU</strong>（0 附近平滑过渡，Transformer 标配）。</li>
    </ul>

    <h2>迭代：用激活把「中间一段」托起来</h2>
    <p>绕了一圈回到第 7 课那堵墙：只看个头大小 x，好苹果挤在中间一段（太小、太大都不是），单个神经元只有一个分界点框不住。现在就用激活函数跨过去。设合格区间 <code>0.4 ≤ x ≤ 0.7</code>。第一层放<strong>两个</strong>神经元，各盯一个边界，激活先用最直白的阶跃：<code>h₁ = 阶跃(x−0.4)</code>（过 0.4 就亮）、<code>h₂ = 阶跃(x−0.7)</code>（过 0.7 也亮）；第二层相减 <code>y = h₁ − h₂</code>（够大、但还没太大，才判「是」）。代三个数验证：太小 x=0.2→h₁=h₂=0→y=0；适中 x=0.55→h₁=1、h₂=0→y=1；太大 x=0.85→h₁=h₂=1→y=0。全对。点睛之笔全在激活函数上：<strong>若中间不放它</strong>，y=(x−0.4)−(x−0.7)=0.3 是常数，两层又塌回一层；正因为阶跃把两条直线各「折」成 0/1 台阶，第二层一相减才凭空托起中间那一段「带」。再多堆几层、多放神经元，圆、月牙、任意复杂边界都能这样「折」「拼」出来——<strong>开篇那道天花板，到此顶开。</strong></p>

    <h2>总结</h2>
    <p>没有激活函数，那台机器叠再多层也只能切一条直线，表达能力为零。激活函数打破线性叠加的魔咒，让每一层真正增加表达能力；它既要能「弯」、又得够灵敏。Sigmoid 平滑好懂但两端饱和，ReLU 靠 <code>max(0,z)</code> 的一刀正区间永远灵敏，推动了深度学习革命。</p>
    <div class="llm-term"><span class="term-name">「激活函数」</span>是指插在每层线性变换 Wx+b 之后、给网络引入非线性的函数（如 Sigmoid、tanh、ReLU <code>max(0,z)</code> 等），它打破「纯线性多层等价于一层」的诅咒，让层数真正变成表达能力。</div>
  </LlmArticle>
</template>`;export{n as default};
