const n=`<template>
  <LlmArticle>
    <div class="llm-question"><strong>开场问题：</strong>固定窗口看不到更早的词——能不能给网络装上「记忆」？</div>

    <h2>提出问题</h2>
    <p>读这句话补一个空：「小猫没有过马路，因为＿＿太害怕了。」空里该填「它」——指的是前面那只小猫。这对人毫不费力，却难住了上一课的固定窗口模型：「小猫」离空格隔了好几个词。如果模型只看最近 5 个词，它根本瞧不见「小猫」，只能干猜「因为<em>什么</em>太害怕了」，连主语是猫是狗都分不清。</p>
    <div class="llm-box warn">
      <strong>固定窗口的死角</strong>：窗口以外的信息等于不存在。可真实文本里，代词和它指代的对象常常隔着几十个词，段落间的呼应甚至隔着几百个词——窗口再大也兜不住。
    </div>

    <h2>最小方案</h2>
    <p>循环神经网络（RNN）的思路很直接：读每个词时，除了当前这个词，再多收一个输入——来自上一步的「记忆」，记为 <code>h</code>（书上叫「隐藏状态」，这里叫<strong>记忆向量</strong>）。每读一个词做两件事：</p>
    <div class="llm-box hint">
      新记忆 <code>= tanh(W · [当前词向量, 旧记忆] + b)</code>，然后用新记忆去预测下一个词。
    </div>
    <p>拆开看很朴素：把<strong>当前词向量</strong>和<strong>旧记忆</strong>拼在一起，过一个线性变换 <code>W·[]+b</code>，再套一层 <code>tanh</code>（输出在 −1~1 的激活函数），得到新记忆。这个「线性变换 + 激活」被接成了<strong>环</strong>——上一步的输出，又喂回来当这一步的输入。RNN 没造新零件，只是把第 7 课的 <code>Wx+b</code> 和第 8 课的激活函数串成了一个自循环。</p>
    <p>最大的好处是<strong>不限制句子长度</strong>：无论句子多长，每步都只做同一件事——读一个词、更新记忆。100 个词和 10 个词用的是同一套参数 <code>W, b</code>，记忆向量 <code>h</code> 始终在手边，随句子推进不断刷新。</p>

    <h2>手算一遍：记忆怎么一步步更新</h2>
    <p>把公式缩小到能口算：让记忆 <code>h</code> 就是一个数，专门记「这句话里有没有提到小猫」。简化成 <code>h新 = tanh(0.6 × h旧 + x)</code>，其中 0.6 是记忆传递系数，<code>x=1.5</code> 只在读到「小猫」时出现，其余词 <code>x=0</code>。从 <code>h0=0</code> 起步：</p>
    <ol class="llm-steps">
      <li>① 小猫：<code>h = tanh(0.6×0 + 1.5) ≈ 0.90</code> ——记忆被点亮</li>
      <li>② 没有：<code>h = tanh(0.6×0.90 + 0) ≈ 0.50</code></li>
      <li>③ 过　：<code>h = tanh(0.6×0.50 + 0) ≈ 0.29</code></li>
      <li>④ 马路：<code>h = tanh(0.6×0.29 + 0) ≈ 0.17</code></li>
      <li>⑤ 因为：<code>h = tanh(0.6×0.17 + 0) ≈ 0.10</code></li>
    </ol>
    <p>看这串数：<code>0.90 → 0.50 → 0.29 → 0.17 → 0.10</code>。每读一个无关的词，「小猫」的信号都先乘 0.6、再被 tanh 压一下，一步步变矮。到第 5 个词「因为」只剩峰值的约 <strong>11%</strong>——好在 5 个词还撑得住，预测「它」时记忆里多少还残留着一点猫味。</p>

    <h2>发现不足</h2>
    <p>那串递减的数字藏着 RNN 最残酷的毛病：每次刷新记忆都是「旧记忆 × 0.6 + 新词」的混合，旧记忆每走一步都被稀释一点。这就像<strong>打电话传话</strong>——第一个人说「小猫今天心情不好」，传到第二十个人，「小猫」多半已变成「谁？」。隔 5 个词还剩约 0.08，可隔 20 个词：<code>0.6²⁰ ≈ 0.00004</code>，几乎归零。</p>
    <p>从训练角度更严重：要让「第 1 步的小猫」影响「第 20 步预测的它」，需把梯度从第 20 步反向传回第 1 步，每经过一步乘一次权重的导数。只要这个数稍小于 1，连乘 20 次后几乎为零——这就是<strong>梯度消失</strong>，第 1 步的参数根本得不到有效更新。这不是代码写错，是数学上的硬限制：RNN 实践中确实学不到间隔超过 5~10 个词的依赖。</p>

    <h2>迭代</h2>
    <p>「每步无差别混合所有信息」是病根。那如果记忆能<strong>有选择地更新</strong>，重要的多留、无关的少写，会不会好很多？下一课 LSTM 正是沿着这个方向，给记忆装上了「阀门」。</p>

    <h2>总结</h2>
    <p>RNN 给网络加了一个「记忆向量」<code>h</code>，每读一个词就用 <code>h ← tanh(W·[词, 旧记忆] + b)</code> 刷新一次，理论上能记住任意长的上下文。可实践中旧记忆每步被稀释，距离远的词信息几乎消失，梯度也随距离连乘到近乎为零——网络学不到远距离依赖。</p>
    <p>本课要点：固定窗口的死角（窗口外信息等于不存在）；RNN 用记忆向量把「线性变换 + 激活」接成环；手算看到记忆从 0.90 衰减到 0.10；局限是记忆淡出（<code>0.6²⁰≈0.00004</code>）与梯度消失。</p>
    <div class="llm-term"><span class="term-name">「循环神经网络 RNN」</span>是指给网络加一个随读词不断更新的记忆向量 <code>h</code>，把线性变换与激活接成环，从而理论上能利用任意长上下文；但记忆会随距离逐词稀释，梯度也会随距离消失，学不到远距离依赖。</div>
  </LlmArticle>
</template>`;export{n as default};
