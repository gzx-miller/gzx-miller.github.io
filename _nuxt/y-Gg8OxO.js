const n=`<template>
  <LlmArticle>
    <div class="llm-question"><strong>开场问题：</strong>把注意力、FFN、残差拼在一起——2017 年那篇论文到底做了什么？</div>

    <h2>提出问题</h2>
    <p>前两课造出了很厉害的部件：注意力让每个词都能回头看别的词，「它」终于知道指谁；加上多头，模型还能同时盯住指代、语法、情绪这些不同关系。那么问题来了：<strong>既然注意力能让词彼此看见，为什么 Transformer 还要加别的零件？</strong>只靠叠几层注意力不行吗？</p>
    <p>这一课围着一句最简单的话——输入「我 是 一只 小 ___」，目标是预测下一个词——把一台最简 Transformer <strong>亲手搭出来</strong>，看清每个零件是被什么问题逼出来的。</p>

    <h2>最小方案：先把最朴素的版本搭出来</h2>
    <p>先不急着加零件：把「我 / 是 / 一只 / 小」变成向量，丢进一层注意力让每个词去看别的词、把相关信息混进自己，再叠几层，拿末尾位置的向量预测下一个词。这个想法很顺——预测「小 ___」确实需要「我 是 一只 小」的整体信息。</p>
    <div class="llm-box warn">
      <strong>问题卡在这里：注意力能把信息汇总过来，却不等于它已经会「加工」这些信息。</strong>注意力的输出本质是各词 V 的加权平均——它擅长在已有信息间<strong>重新分配比重</strong>，但不擅长凭空造出一个全新的判断。就像第 16 课的 N-gram 只会查表不会推断，注意力负责收集材料，而「搜集来的信息怎么加工」它不管。
    </div>

    <h2>补上第一块零件：FFN</h2>
    <p>这时轮到第二个部件——<strong>FFN（前馈网络）</strong>登场。它不是用来让词和词通信的，而是让<strong>每个词各自</strong>在拿到上下文后做一轮内部加工：</p>
    <div class="llm-box hint">
      <code>FFN(x) = W₂·ReLU(W₁x + b₁) + b₂</code>。分工很清晰：<strong>注意力</strong>是横向的，负责「去看别人」；<strong>FFN</strong> 是纵向的，负责「拿到信息后自己再想一遍」。
    </div>
    <p>论文里 FFN 常写成 <code>512 → 2048 → 512</code>：先把维度<strong>撑大到 4 倍</strong>、过一道 ReLU、再<strong>压回</strong>原样。为什么要这么折腾？FFN 拿到的「小」已经不是词表里那个孤零零的「小」，而是刚过完注意力、已把「我 是 一只」线索揉进来的 512 维向量——只是这些线索挤成一团、藏在数字里没被读出来。FFN 要做的，就是把暗藏线索一条条识别出来，判断「下一个词会不会是某种小动物」：撑到 2048 维，就是摆开两千多个这样的「小检测器」，每个查一条线索；ReLU 把查到的留下、没查到的清零；第三个矩阵把亮着的线索汇总成结论、压回 512 维交出去。</p>
    <p>所以两次变维各有用处：<strong>撑大</strong>是因为 512 维塞不下这么多检测器；<strong>压回</strong>是因为下一个 block 接的就是 512 维。至于偏偏是 4 倍，是试出来的经验值。</p>

    <h2>把它们拼起来：最简 Transformer 主干</h2>
    <p>到一个词进来，先和别的词交换信息、再自己做一轮加工，两个步骤首尾相接就是一个 Transformer block。这个 block 有个妙性质：<strong>输入和输出维度一样</strong>。4 个词、每个词 512 维，一个 block 吃进 <code>4 × 512</code>、吐出的还是 <code>4 × 512</code>——只是每个位置的内容更新过了。更一般地，block 处理的是 <code>n × 512</code> 一整张表，n 是实际 token 数，唯一的限制是 n 不能超过模型允许的最大上下文长度。</p>
    <ol class="llm-steps">
      <li>把「我 / 是 / 一只 / 小」查词表，变成一排词向量。</li>
      <li>过几层 block，反复交换信息、反复加工。</li>
      <li>取最后一个位置的输出向量，经线性层 + softmax，预测「下一个词是谁」——得到「猫 31%、狗 22%、鸟 12%…」。</li>
    </ol>
    <p>这就是 GPT 式生成的核心：挑一个结果接到句尾，再把整句重新送进去继续预测，文本就这样一个词一个词长出来。主抓到了：<strong>Transformer block = attention + FFN，且可以堆叠</strong>。</p>

    <h2>发现不足：模型不知道谁在前谁在后</h2>
    <p>问题恰好出在我们刚夸过的优点——<strong>并行</strong>上。Transformer 把一整排词同时送进去算，不像 RNN 一个个排队，所以特别适合 GPU、适合做大。但「同时处理」也带来副作用：<strong>如果只给词向量、不额外告诉位置，注意力本身并不知道顺序</strong>。「猫 追 狗」和「狗 追 猫」用的是同一批词、意思却完全相反；模型只看一袋词和向量，根本不知道谁在主语还是宾语位置。</p>

    <h2>迭代：补上位置编码</h2>
    <p>这就逼出第三块零件——<strong>位置编码</strong>。方法很直接：在送进第一层注意力之前，先给每个词向量加上一份「我是第几个」的信息，即 <code>真正的输入 = 词向量 + 位置向量</code>。这样一来「猫@位置0」和「猫@位置2」就不再是同一个向量，「猫追狗」和「狗追猫」终于能被区分开。原始论文用正弦 / 余弦位置编码，后来的模型还有可学习位置向量、RoPE 等做法——实现可以变，但目的始终没变：<strong>告诉模型这些词不仅有内容，还有先后次序</strong>。</p>

    <h2>总结</h2>
    <p>一台能跑的 Transformer 主干已拼好：<strong>词向量</strong>告诉「这是什么词」；<strong>位置编码</strong>告诉「它排第几个」；<strong>注意力</strong>让词与词交换信息；<strong>FFN</strong> 让每个词拿到信息后再加工一遍；<strong>多层堆叠</strong>把这件事反复做深；<strong>末尾线性层 + softmax</strong> 把最后一个位置变成下一个词的概率表。它能并行、能堆深、能做「预测下一个词」，正是 GPT 做大起来的出发点。本课有意省掉了 Add & Norm、掩码等细节，留到后面单独讲。</p>
    <div class="llm-term"><span class="term-name">「Transformer」</span>是指以「多头注意力（横向通信）+ 前馈网络 FFN（纵向加工）」为一个可堆叠的 block，加上位置编码把顺序塞回输入，再经线性层与 softmax 输出下一个词概率的架构；由于并行计算、能建模长距离依赖，它取代了 RNN/LSTM，成为 GPT、BERT 等所有大模型的地基。</div>
  </LlmArticle>
</template>`;export{n as default};
