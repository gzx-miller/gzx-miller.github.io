<script setup lang="ts"></script>

<template>
  <LlmArticle>
    <div class="llm-question"><strong>开场问题：</strong>神经元叠成一张网，再让它自己「学」——「训练」到底在做什么？</div>

    <h2>提出问题</h2>
    <p>前面几课我们造好了零件：<strong>神经元</strong>（加权求和 + 偏置 + 激活）和<strong>激活函数</strong>。可手里始终是零件——这一课把它们拼成整机：<strong>神经网络</strong>，并讲清楚它是怎么「学」会本事的。先把第 7 课见过的那排神经元收编：给每种水果配一个神经元并排站好，再用 softmax 把分数压成概率——这「一排神经元」，就是<strong>一层</strong>。</p>

    <h2>最小方案</h2>
    <p>关键一步：<strong>把前一层的输出，当作后一层的输入</strong>，一层接一层叠下去。最前面的叫<strong>输入层</strong>，最后给出答案的叫<strong>输出层</strong>，夹在中间、不直接和外界打交道的统称<strong>隐藏层</strong>。节点是神经元、连线是权重；<strong>层数</strong>叫网络的「深度」，每层神经元个数叫「宽度」。层与层之间必须夹着激活函数，否则再深也等于一层。</p>
    <h3>整张网络其实是一个带参数的大函数</h3>
    <p>每层做的都是同一件事：先做线性变换 <code>Wx + b</code>，再过一道激活函数，把结果交给下一层。整张网络不过是把这套动作重复几次，即 <code>ŷ = f(x; θ)</code>，其中 <code>θ = {W₁,b₁, W₂,b₂, …}</code> 是所有权重与偏置的总和，也就是网络的<strong>参数</strong>。上一张 4→5→5→4 的小网就有 79 个参数；GPT-3 则有 <strong>1750 亿</strong>个。</p>

    <h2>发现不足</h2>
    <ul>
      <li>几百甚至上千亿个参数不可能像第 7 课那样一个个手工去猜。</li>
      <li>结构（几层、几个神经元）定下了网络<em>能</em>表达多复杂的规律，但具体表达成什么样，完全取决于这些数取什么值。</li>
    </ul>

    <h2>迭代</h2>
    <p>网络刚出生时参数是<strong>随机</strong>的，喂它一个苹果多半会瞎猜。所谓<strong>训练（training）</strong>，就是拿一大批已知正确答案（带标签）的数据，一遍遍微调参数，让预测越来越贴近真实答案。靠的是一个不断重复的循环：<strong>① 前向计算</strong>算出预测 → <strong>② 算损失</strong>量出错得多离谱 → <strong>③ 求梯度</strong>每个参数该往哪调 → <strong>④ 更新参数</strong> <code>θ ← θ − 学习率 × 梯度</code>。重复成千上万次，损失一点点变小、参数一点点变好。</p>
    <div class="llm-box hint">▶ 训练循环，每一块都是一门专门的课：② 算损失 → 第 10 课《损失函数》；④ 更新参数 → 第 12 课《梯度下降与优化器》；③ 求梯度 → 第 13 课《反向传播》。</div>
    <p>还要区分两个常被混淆的词：<strong>训练</strong>是「调参数」的过程，要算损失和梯度、很费算力；训练完成后参数<strong>固定</strong>下来，拿去对新数据做预测，叫<strong>推理（inference）</strong>——你平时用 ChatGPT，用的正是它训练好、参数冻住后的样子。</p>

    <h2>总结</h2>
    <p>把神经元一层层叠起来、前一层输出喂给后一层，就成了神经网络。整张网络本质是一个带参数 <code>θ</code> 的大函数，它的全部本事都藏在权重和偏置里。训练，就是用带答案的数据，靠「前向 → 算损失 → 求梯度 → 更新参数」的循环，让网络自己把参数调到最好。</p>
    <div class="llm-term"><span class="term-name">「神经网络」</span>是指把多个神经元按输入层、隐藏层、输出层层层堆叠、并把前一层的输出作为后一层输入的网络结构；整张网络等价于一个由参数 <code>θ</code> 决定的大函数，它的所有判断能力都藏在这堆权重和偏置里。</div>
  </LlmArticle>
</template>