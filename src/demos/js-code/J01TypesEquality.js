// 值类型与隐式转换
const a = '0'    // 字符串
const b = 0      // 数字

// 宽松相等：触发隐式类型转换，'0' 被转为数字 0
console.log(a == b)   // true

// 严格相等：不转换类型，字符串 !== 数字
console.log(a === b)  // false

// 业务建议：始终使用 === 避免意外转换
// 需要转换时显式完成：Number(a) === b
